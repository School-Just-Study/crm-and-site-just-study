import { Settings } from 'react-slick';

export const settings: Settings = {
    dots: false,
    arrows: false,
    swipeToSlide: true,
    speed: 500,
    lazyLoad: 'ondemand',
    slidesToScroll: 1,
    adaptiveHeight: true,
    infinite: true,
    centerMode: true,
    autoplay: true,
    autoplaySpeed: 5000
};
