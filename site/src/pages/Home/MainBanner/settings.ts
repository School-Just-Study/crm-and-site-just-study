import { Settings } from 'react-slick';

export const settings: Settings = {
    dots: false,
    arrows: false,
    lazyLoad: 'ondemand',
    // fade: true,
    swipeToSlide: false,
    centerMode: true,
    autoplay: true,
    autoplaySpeed: 3000,
    slidesToShow: 1,
    slidesToScroll: 1
};
